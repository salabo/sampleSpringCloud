
server:
  port: 8090
eureka:
  instance:
    instance-id: myservice
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8080/eureka/  # 注册中心访问地址
spring:
  application:
    name: myservice
  datasource:
    data-password: 123
    data-username: root